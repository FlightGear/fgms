- precompile list of clients in a list of strings at regular intervals,
  show this list on telnet port
- make relay list available via telnet
- let relays introduce themself (name, location, email)
- create an IRC like chat protocoll
- fgms git repository back to SF?
- implement server to server communication
  special messages for server internal purposes
- rate limit clients
  clients sents X pakets per second, but we only forward Y pakets per
  second
- do not relay entries where flight_id is NULL, latitude is 0 
  or longitude is 0
